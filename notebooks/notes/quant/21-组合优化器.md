# 组合优化器

#组合优化 #有效前沿 #Markowitz #资产配置

## 核心概念

### 什么是组合优化？

组合优化是**在给定风险水平下最大化收益**，或**在给定收益水平下最小化风险**的过程。

核心公式（Markowitz 均值-方差模型）：

$$\min_w \frac{1}{2} w^T \Sigma w$$

$$\text{s.t.} \quad w^T \mu = \mu_p, \quad w^T \mathbf{1} = 1, \quad w_i \geq 0$$

### 有效前沿

有效前沿是所有**风险-收益最优组合**的集合：

- 对于每个风险水平，有效前沿上的组合提供最高收益
- 对于每个收益水平，有效前沿上的组合提供最低风险
- 理性投资者只会选择有效前沿上的组合

### 关键组合

| 组合 | 特点 | 适用场景 |
|------|------|----------|
| **最小方差组合** | 风险最低 | 风险厌恶型投资者 |
| **最大夏普比组合** | 风险调整后收益最高 | 追求性价比的投资者 |
| **等权组合** | 每只股票权重相等 | 简单分散化 |
| **市值加权组合** | 按市值分配权重 | 被动指数投资 |

---

## 选股逻辑

### 多条件筛选标准

1. **市值要求**：选择大中盘股票（市值 > 中位数）
2. **盈利能力**：ROE > 行业平均
3. **估值合理**：PE < 行业平均
4. **流动性**：日均成交额 > 1000 万

### 综合得分

```python
score = 0.3 * ROE标准化 + 0.3 * (1-PE标准化) + 0.2 * 市值标准化 + 0.2 * 流动性标准化
```

---

## 约束条件

### 为什么需要约束？

| 约束 | 数学形式 | 业务含义 |
|------|----------|----------|
| **权重非负** | $w_i \geq 0$ | 不允许做空（多数公募基金限制） |
| **权重上限** | $w_i \leq 30\%$ | 避免过度集中于单只股票 |
| **权重和为1** | $\sum w_i = 1$ | 全额投资，不持有现金 |
| **行业分散** | $\sum_{i \in \text{industry}} w_i \leq 40\%$ | 避免行业集中风险 |

### 约束对优化结果的影响

- 无约束组合可能包含做空头寸
- 约束条件会降低优化效率，但更符合实际投资限制
- 权重上限 30% 是为了避免过度集中风险

---

## 完整代码模式

### 组合统计量计算

```python
def portfolio_stats(weights, returns, cov_matrix):
    """计算组合的收益率、波动率和夏普比"""
    port_return = np.dot(weights, returns)
    port_volatility = np.sqrt(np.dot(weights.T, np.dot(cov_matrix, weights)))
    sharpe_ratio = port_return / port_volatility
    return port_return, port_volatility, sharpe_ratio
```

### 最小方差组合

```python
def min_variance(returns, cov_matrix, target_return=None):
    """最小方差组合"""
    n = len(returns)
    init_weights = np.array([1/n] * n)
    
    constraints = [{'type': 'eq', 'fun': lambda w: np.sum(w) - 1}]
    if target_return is not None:
        constraints.append({'type': 'eq', 'fun': lambda w: np.dot(w, returns) - target_return})
    
    bounds = tuple((0, 0.3) for _ in range(n))
    
    def objective(w):
        return np.dot(w.T, np.dot(cov_matrix, w))
    
    result = minimize(objective, init_weights, method='SLSQP',
                      bounds=bounds, constraints=constraints)
    return result.x
```

### 最大夏普比组合

```python
def max_sharpe(returns, cov_matrix):
    """最大夏普比组合"""
    n = len(returns)
    init_weights = np.array([1/n] * n)
    
    constraints = [{'type': 'eq', 'fun': lambda w: np.sum(w) - 1}]
    bounds = tuple((0, 0.3) for _ in range(n))
    
    def objective(w):
        port_return = np.dot(w, returns)
        port_volatility = np.sqrt(np.dot(w.T, np.dot(cov_matrix, w)))
        return -port_return / port_volatility
    
    result = minimize(objective, init_weights, method='SLSQP',
                      bounds=bounds, constraints=constraints)
    return result.x
```

### 有效前沿生成

```python
target_returns = np.linspace(annual_returns.min(), annual_returns.max(), 50)
frontier_volatilities = []
frontier_returns = []

for target in target_returns:
    try:
        weights = min_variance(annual_returns.values, cov_matrix.values, target_return=target)
        ret, vol, _ = portfolio_stats(weights, annual_returns.values, cov_matrix.values)
        frontier_returns.append(ret)
        frontier_volatilities.append(vol)
    except:
        pass
```

### 回溯测试

```python
def backtest_portfolio(returns_df, weights):
    """回溯测试组合表现"""
    portfolio_returns = (returns_df * weights).sum(axis=1)
    cumulative_returns = (1 + portfolio_returns).cumprod()
    peak = cumulative_returns.expanding().max()
    drawdown = (cumulative_returns - peak) / peak
    
    return {
        'returns': portfolio_returns,
        'cumulative': cumulative_returns,
        'drawdown': drawdown,
        'total_return': cumulative_returns.iloc[-1] - 1,
        'annual_return': (cumulative_returns.iloc[-1] ** (252/len(returns_df))) - 1,
        'annual_volatility': portfolio_returns.std() * np.sqrt(252),
        'sharpe_ratio': (portfolio_returns.mean() * 252) / (portfolio_returns.std() * np.sqrt(252)),
        'max_drawdown': drawdown.min()
    }
```

---

## 可视化

### 权重饼图

```python
fig, axes = plt.subplots(1, 3, figsize=(15, 5))
for idx, (name, weights) in enumerate(portfolios.items()):
    ax = axes[idx]
    significant = weights > 0.02
    plot_weights = weights[significant]
    plot_names = [stock_names[i] for i in range(len(stock_names)) if significant[i]]
    ax.pie(plot_weights, labels=plot_names, autopct='%1.1f%%')
    ax.set_title(name)
```

### 有效前沿图

```python
fig, ax = plt.subplots(figsize=(12, 8))
ax.plot(frontier_volatilities, frontier_returns, 'b-', linewidth=3, label='有效前沿')
# 标记各组合
for name, weights in portfolios.items():
    ret, vol, sharpe = portfolio_stats(weights, annual_returns.values, cov_matrix.values)
    ax.scatter(vol, ret, s=200, label=f'{name} (SR={sharpe:.2f})')
```

### 累积收益对比

```python
fig, ax = plt.subplots(figsize=(14, 6))
for name, result in backtest_results.items():
    ax.plot(result['cumulative'].index, result['cumulative'], label=name, linewidth=2)
ax.set_title('组合累积收益对比')
ax.legend()
```

---

## 踩坑记录

### 1. 协方差矩阵不正定
- **问题**：优化求解失败
- **原因**：协方差矩阵奇异（样本量太小或股票高度相关）
- **解决**：使用收缩估计器（Ledoit-Wolf）或增加样本量

### 2. 优化结果不稳定
- **问题**：权重变化剧烈
- **原因**：对输入参数（预期收益率）敏感
- **解决**：使用稳健优化或 Black-Litterman 模型

### 3. 样本内过拟合
- **问题**：样本内表现很好，样本外很差
- **原因**：过度优化历史数据
- **解决**：使用滚动窗口优化，关注样本外表现

### 4. 交易成本忽略
- **问题**：实际收益低于回溯结果
- **原因**：未考虑交易费用和冲击成本
- **解决**：在优化中加入换手率约束

---

## 对比：不同优化方法

| 方法 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **均值-方差** | 理论基础扎实 | 对输入敏感 | 学术研究 |
| **最小方差** | 不需要预期收益估计 | 可能收益较低 | 风险厌恶型 |
| **风险平价** | 分散化好 | 不考虑收益 | 多资产配置 |
| **Black-Litterman** | 结合主观观点 | 复杂 | 专业投资者 |

---

## 相关笔记

- [[12-CAPM贝塔系数]] - CAPM 理论基础
- [[13-Fama-French三因子模型]] - 因子模型
- [[14-Fama-French五因子模型]] - 五因子模型
- [[20-Markowitz-均值方差优化]] - Markowitz 理论

---

## 对应 Notebook

`21_portfolio_optimizer.ipynb`

---

## 顺序通关导航

- 上一课：[[20-Markowitz-均值方差优化]]
- 下一课：[[22-均线策略与参数优化]]
- 对应 Notebook：`21_portfolio_optimizer.ipynb`

## 本课复习检查点

- [ ] 我能用自己的话解释本课核心概念。
- [ ] 我能从上到下运行对应 Notebook。
- [ ] 我能改一个参数或场景，并解释结果变化。
